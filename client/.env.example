# Copy to .env.local. Vite only exposes vars prefixed with VITE_.
#
# Leave this unset in development — vite.config.js proxies /api to the
# Flask API on port 5000. Set it when the API lives elsewhere.
# VITE_API_URL=https://api.jiranihub.example.com/api
